# OpenRouter free-tier API key (DeepSeek model id pinned in src/reasearch_crew/config/llm.json).
# Copy this file to .env in the repo root and fill in the value. Never commit .env.
OPENROUTER_API_KEY=
# Google AI Studio free key
GEMINI_API_KEY=
